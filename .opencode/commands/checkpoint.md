---
description: Create one focused local Git commit without pushing.
agent: git-steward
---

Create a local Git checkpoint for: $ARGUMENTS

Inspect the working tree first. Do not push. Stop if unrelated changes make the intended files unclear.
